# Cloned upstream ground truth: 2.1 GB, each with its own .git. Re-fetch with tools/fetch-seeds.sh
corpus/raw/
# Derived from corpus/raw by tools/ingest.py -- regenerate, don't commit
corpus/normalized/
.DS_Store
__pycache__/
# generated by tools/build_exercises.py + tools/make_trainer.py (3 MB each)
build/
corpus/crops/
site/read/
site/.vercel
.vercel
# Nothing here needs npm: analytics is the static /_vercel/insights/script.js tag, and a
# root package.json makes Vercel treat this as a Node project and build it (research/deploy.md)
node_modules/
# Draft outreach emails. Gitignored on purpose: this repo is PUBLIC, and unsent drafts
# naming people who have not agreed to anything do not belong in it.
outreach/
