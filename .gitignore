# Cloned upstream ground truth: 2.1 GB, each with its own .git. Re-fetch with tools/fetch-seeds.sh
corpus/raw/
# Derived from corpus/raw by tools/ingest.py -- regenerate, don't commit
corpus/normalized/
.DS_Store
__pycache__/
# generated by tools/build_exercises.py + tools/make_trainer.py (3 MB each)
build/
corpus/crops/
site/read/
site/.vercel
.vercel
# @vercel/analytics install; the site loads analytics from /_vercel/insights/script.js
# (the static-site route), so nothing here is served -- npm i to restore
node_modules/
